schema_version: "1.0"
project: system-modeller
plan:
  id: C
  name: Multi-runtime distributions
  document: docs/PLAN-C-multi-runtime-distributions.md
progress:
  completed_step: C7
  total_steps: 7
  state: complete
version: 0.1.0-dev.57
milestone: Plan C complete – four-runtime architecture, unified build, CI/release publication and final hygiene verified.
runtime_status:
  chat: ready
  custom_gpt: ready
  claude: ready
  opencode: ready
notes:
  - Chat, Custom GPT, Claude Project and OpenCode are generated from the same canonical project/runtime contract.
  - Unified build produces project plus all four runtime ZIPs, runtime parity, checksums and build manifest.
  - CI and GitHub Release publish the complete validated artifact set.
  - Final C7 work is documentation/hygiene only and does not change canonical runtime behavior.
