schema_version: "1.0"
project: system-modeller
plan:
  id: C
  name: Multi-runtime distributions
  document: docs/PLAN-C-multi-runtime-distributions.md
progress:
  completed_step: C4
  total_steps: 7
  state: in_progress
version: 0.1.0-dev.54
milestone: OpenCode workspace distribution implemented and compatibility-validated.
next_step:
  id: C5
  title: Unified build and validation
  objective: >
    Integrate all ready runtime distributions into one deterministic build path
    with manifest, parity report and SHA-256 checksums.
runtime_status:
  chat: ready
  custom_gpt: ready
  claude: ready
  opencode: ready
notes:
  - OpenCode generates root AGENTS.md, V2 opencode.json permissions and project-local typed custom tools.
  - system_model requires approval; read-only/derived System Modeller tools are allowed.
  - The target System Modeller project is separate from the runtime workspace and is addressed through projectRoot.
  - Claude and OpenCode become active unified build targets in C5.
