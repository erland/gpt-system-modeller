schema_version: "1.0"
project: system-modeller
plan:
  id: C
  name: Multi-runtime distributions
  document: docs/PLAN-C-multi-runtime-distributions.md
progress:
  completed_step: C5
  total_steps: 7
  state: in_progress
version: 0.1.0-dev.55
milestone: Unified deterministic build and validation for project package and four runtimes established.
next_step:
  id: C6
  title: CI and GitHub Release
  objective: >
    Publish and validate all unified build artifacts in pull-request/main workflows
    and attach all publishable runtime assets, parity and checksums to GitHub Releases.
runtime_status:
  chat: ready
  custom_gpt: ready
  claude: ready
  opencode: ready
notes:
  - All four ready runtimes are active build targets.
  - ci_build.py produces project, Chat, Custom GPT, Claude and OpenCode ZIPs plus parity and checksums.
  - Existing runtime-specific packaging commands remain available as compatibility entry points.
