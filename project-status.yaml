schema_version: "1.0"
project: system-modeller
plan:
  id: C
  name: Multi-runtime distributions
  document: docs/PLAN-C-multi-runtime-distributions.md
progress:
  completed_step: C3
  total_steps: 7
  state: in_progress
version: 0.1.0-dev.53
milestone: Claude Project distribution implemented and compatibility-validated.
next_step:
  id: C4
  title: OpenCode workspace distribution
  objective: >
    Implement the OpenCode adapter with generated AGENTS.md, runtime contract,
    declared custom tools and approval rules for mutating operations.
runtime_status:
  chat: ready
  custom_gpt: ready
  claude: ready
  opencode: planned
notes:
  - Claude Project packages canonical behavior and selected Knowledge.
  - Claude local script execution is explicitly unavailable; canonical mutation requires reliable validation.
  - Claude remains disabled as a unified build target until C5 integrates all active distribution builds.
